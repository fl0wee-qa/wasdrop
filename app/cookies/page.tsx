export default function CookiesPage() {
  return (
    <article className="prose prose-invert max-w-3xl text-zinc-300">
      <h1 className="text-3xl text-zinc-50">Cookie Policy</h1>
      <p>
        Essential cookies are used for authentication sessions and country/region preferences. Optional analytics cookies
        are disabled by default in MVP.
      </p>
      <p>
        Cookie consent choices are stored in browser localStorage and can be changed by clearing site storage.
      </p>
    </article>
  );
}
